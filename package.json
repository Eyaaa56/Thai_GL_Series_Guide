{
  "name": "roseframe",
  "private": true,
  "version": "1.0.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "npm --prefix client ci && npm --prefix client run build",
    "test": "npm --prefix server test"
  },
  "dependencies": {
    "@vercel/blob": "^2.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.9.3"
  }
}
